package com.example.order_api.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class KafkaConfig {
    // Keep for future customization (transactions, interceptors, etc.)
}
